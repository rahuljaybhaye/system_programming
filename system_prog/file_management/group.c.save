#include<sys/types.h>
#include<grp.h>
#include<stdio.h>

main()
{

	struct group *g;
	char g_name[] = "adm";          //"students";
	g = getgrnam(g_name);

	printf("gr_name: %s\n",g->gr_name);
	printf("gr_passwd: %s\n",g->gr_passwd
	printf("gr_gid: %d\n",g->gr_gid);

//	char arr[100];
//	arr g->gr_mem
//	printf("gr_mem: %s\n",*(g->gr_mem));

	for()


}
